loop         {usage : n!loop (looping current song)}
lyrics       {usage : n!lyrics (lyrics of any song by searching)}
nowplaying   {usage : n!nowplaying or .np (song on playing this time)}
pause        {usage : n!pause (pause song)}
play         {usage : n!play (play song by typping song name)}
pruning      {undefiend}
queue        {usage : n!queue (songs queue)}
remove       {usage : n!remove (remove songs by typing)}
resume       {usage : n!resume (resume currently played song)}
search       {usage : n!search (search any song)}
shuffle      {usage : n!shuffle(shuffle any songs)}
skip         {usage : n!skip (skip current played song)}
skipto       {usage : n!skip (skip song by typing number in queue)}
stop         {usage : n!stop (stop song)}
volume       {usage : n!volume (adjust volume of song)}
